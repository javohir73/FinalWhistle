// Floodlight P6: the canonical /football/<comp>/team/<id> URL wraps the legacy
// team page (see page.tsx), so it reuses the same cold-start skeleton — matching
// the max-w-3xl column and crest banner.
export { default } from "@/app/team/[id]/loading";
