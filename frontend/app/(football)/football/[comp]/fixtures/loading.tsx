// Floodlight P6: the canonical /football/<comp>/fixtures URL wraps the legacy
// fixtures page (see page.tsx), so it reuses the same cold-start skeleton rather
// than forking one — same box, same shimmer.
export { default } from "@/app/matches/loading";
