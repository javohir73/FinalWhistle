# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Config / validation
pydantic==2.10.4
pydantic-settings==2.7.1

# Error tracking (only active when SENTRY_DSN is set)
sentry-sdk[fastapi]==2.20.0

# Data / ML (used by the ml/ and pipeline/ packages)
pandas==2.2.3
numpy==2.2.1
scikit-learn==1.6.0
requests==2.32.3

# Testing
pytest==8.3.4
httpx==0.28.1  # FastAPI TestClient dependency
