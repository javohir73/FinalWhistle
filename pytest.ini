[pytest]
# Python tests live alongside code as *_test.py, plus backend/tests and ml/pipeline packages.
python_files = *_test.py test_*.py
testpaths = backend ml pipeline
# Make the FastAPI app ("app.*") and the ml/pipeline packages importable in tests.
pythonpath = backend .
addopts = -q
